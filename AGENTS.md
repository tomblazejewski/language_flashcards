## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues on `tomblazejewski/language_flashcards`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default label strings are used for all five triage roles. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo — one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
